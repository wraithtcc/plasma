[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "plasma-visual"
version = "1.0.0"
description = "Fluid plasma fields, rendered frame by frame in the shell"
readme = "README.md"
requires-python = ">=3.8"
dependencies = []

[project.scripts]
plasma-visual = "main:main"
